name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  library-and-examples:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24, 26]
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node }}
          cache: "pnpm"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.8"
          channel: stable
          cache: true

      - uses: dtolnay/rust-toolchain@stable

      - name: Install Rust minimum-supported toolchain
        run: rustup toolchain install 1.88.0 --profile minimal

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Validate release contract
        run: pnpm run ci:validate

      - name: Lint
        run: pnpm run ci:lint

      - name: Typecheck (all workspaces)
        run: pnpm run ci:typecheck

      - name: Build all workspaces
        run: pnpm run ci:build

      - name: Test
        run: pnpm run ci:test

      - name: Verify skills ↔ exports ledger
        run: pnpm run ci:skills

      - name: Audit production dependency policy
        if: matrix.node == 24
        run: pnpm run ci:security
