---
"@prometheus-ags/prometheus-entity-management": patch
---

Keep `useEntityView` and `useEntityQuery` item projections reactive when an
existing normalized entity changes without changing the view's ID list.
