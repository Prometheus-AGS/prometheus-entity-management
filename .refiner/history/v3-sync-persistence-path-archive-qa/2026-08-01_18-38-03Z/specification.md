# Specification

```yaml
artifact_name: v3-sync-persistence-path-archive-qa
artifact_type: content
content_type: direct:content
clarified_intent:
  description: Challenge the mandatory PGlite/Loro archive claim against every phase-plan detail, executable receipt, package boundary, explicit sibling opt-in rule, and remaining release limitation.
  domain: release verification content and code contract
target_state:
  description: A reproducible archive decision proving durable storage, deterministic peer convergence, lossless reconnect, packed consumers, and synchronized ledgers without overclaiming platform, visual, relay, or publication readiness.
  success_criteria:
    - A real PGlite filesystem database closes and reopens with canonical entities, intentional patches, and ID-only lists.
    - Two isolated Loro graph clients preserve different-field offline writes and deterministically resolve same-field conflicts under FIFO and reverse delivery.
    - Inbound peer projections do not echo and disconnected WebSocket writes recover after a real socket termination.
    - Mandatory tests install exact dependencies and contain no conditional skip, todo, environment bypass, or availability early return.
    - Packed core/sync consumers and the broader twelve-package contract pass.
    - The sibling prometheus-entity-sync check is manual-only, consumes a packed current core, and cannot replace mandatory local evidence.
    - Coverage, exports, skills, docs, and release limitations are synchronized; publication and visual claims remain honest.
unknowns: []
requires_code_execution: true
likely_tools:
  - pnpm
  - actionlint
  - strict OpenSpec validation
  - filesystem and JSON validation
```
