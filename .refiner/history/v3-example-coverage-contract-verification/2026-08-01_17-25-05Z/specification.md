# Specification

```yaml
artifact_name: v3-example-coverage-contract-verification
artifact_type: content
content_type: direct:content
clarified_intent:
  description: Challenge the shared example semantic contract and its archive evidence against every phase-plan requirement without confusing planned showcase mappings with implemented runtime, visual, platform, or full-release proof.
  domain: release verification content
target_state:
  description: A concise, reproducible archive decision that proves the semantic fixture, scenario, coverage, and fail-closed validator contract while preserving all downstream implementation boundaries.
  success_criteria:
    - The shared domain contains exactly Project, User, Task, Comment, and Activity with deterministic IDs, fixed time, synthetic tenancy, and keyless transports.
    - Every stable capability and release artifact maps to executable semantic evidence and at least one declared showcase.
    - The validator fails closed for missing, stale, duplicate, malformed, or nonexistent implemented evidence.
    - All examples share scenario IDs, fixtures, expected outcomes, normalization rules, and security assumptions even though presentation may differ.
    - Coverage remains in progress, all five showcase runtime and visual claims remain planned, and stable publication remains unauthorized.
    - Every evidence path exists, every machine report parses, strict OpenSpec validation passes, and no archive task remains unchecked.
unknowns: []
requires_code_execution: true
likely_tools:
  - shell validation
```

