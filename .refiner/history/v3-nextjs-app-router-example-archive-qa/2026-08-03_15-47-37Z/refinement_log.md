# Refinement log — `v3-nextjs-app-router-example-archive-qa`

## Cycle 2, iteration 1 — 2026-08-03

### Delta first

The first isolated review invalidated cycle 1's “no implementation defect”
conclusion. It found stale task metadata and a real runtime ownership gap:
provider-owned graphs never received garbage collection. This iteration fixes
both. It does not certify npm publication, stable 3.0, untested browsers or
rendering modes, or the remaining showcase portfolio.

### Specify

Retained the eight blocking KBD/OpenSpec constraints and added explicit success
criteria for task-5 report identity and selected-versus-sibling graph GC.

### Plan

Correct source and report metadata, add the focused regression, synchronize
API guidance, rerun the full packed production/browser boundary, recompute
hashes, validate ledgers and release rules, then persist a new named cycle.

### Execute

- Replaced one process-wide GC timer with one timer per selected graph while
  preserving the default singleton API.
- Added a focused regression proving that collecting one graph leaves a sibling
  graph unchanged.
- Changed the generated report metadata from task 3 to task 5.
- Updated advanced, package, and skill API guidance for optional graph scope.
- Passed all 10 packed-verifier commands with the corrected core tarball, 13/13
  focused units, 5/5 structural checks, 12/12 isolated requests, 2/2 browser
  flows, and zero serious or critical accessibility findings.
- Recomputed all six retained evidence hashes and revalidated 203 React exports,
  13/13 semantic scenarios, 14/14 coverage tests, strict OpenSpec, Changesets,
  release contract, frozen install, diff hygiene, and security.

### Reflect

The material delta is runtime and evidence correction, not process paperwork.
Both review warnings now have direct tests or machine-readable proof. No
critical or blocking constraint remains, but a new full-diff adversarial review
is mandatory because the reviewed artifact changed.

### Persist

Persisted cycle-2 specification, plan, constraints, reflection, validation,
archive report, decision, and converged state under refinement ID
`46725a52-9c0b-409b-bf20-26eb6447cb8e`.
