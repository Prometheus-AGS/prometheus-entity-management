{
  "plan": {
    "stages": [
      {"id": "s1", "description": "Map the latest critical to copied-tree filtering, complete alias scanning, receipt metadata, and structural regression coverage.", "requires_execution": false},
      {"id": "s2", "description": "Run focused lint, type, structural, and provider-replacement tests plus the exact no-argument tarball-only verifier with the complete alias scan.", "requires_execution": true, "tool": "focused repository commands"},
      {"id": "s3", "description": "Recompute retained hashes and validate coverage, exports, OpenSpec, Changesets, release contract, frozen install, security, and diff hygiene.", "requires_execution": true, "tool": "focused repository commands"},
      {"id": "s4", "description": "Evaluate unresolved limits and the React-first/future-Next release separation.", "requires_execution": false},
      {"id": "s5", "description": "Persist, validate, checkpoint, and finalize artifact-refiner cycle 5.", "requires_execution": true, "tool": "artifact-refiner scripts"}
    ],
    "tool_invocations": [
      {"stage_id": "s2", "tool": "focused repository commands", "purpose": "Prove every copied command-relevant text file is alias-free and the prior lifecycle corrections remain certified."},
      {"stage_id": "s3", "tool": "focused repository commands", "purpose": "Prove ledgers, hashes, dependency state, and release boundaries remain synchronized."},
      {"stage_id": "s5", "tool": "artifact-refiner scripts", "purpose": "Validate schemas, file references, state consistency, and durable history."}
    ],
    "state_updates": {
      "files_to_create": [],
      "files_to_update": ["specification.json", "plan.json", "constraints.json", "artifact_manifest.json", "reflection.json", "refinement_log.md", "decisions.md", "dist/archive-qa-report.md", "dist/validation.json", "state.json", ".refiner/registry.json"],
      "directories_required": ["dist"]
    },
    "validation_plan": [
      {"constraint_id": "acceptance-direct-evidence", "validation_method": "latest critical and acceptance matrix predicates", "execution_required": true},
      {"constraint_id": "packed-consumer-boundary", "validation_method": "source-only exclusion, 112-file alias scan, exact-path failure, and tarball-only verifier", "execution_required": true},
      {"constraint_id": "ssr-hydration-runtime", "validation_method": "provider replacement, adapter cleanup, lifecycle, and packed browser assertions", "execution_required": true},
      {"constraint_id": "visual-accessibility-integrity", "validation_method": "fresh hash and axe assertions", "execution_required": true},
      {"constraint_id": "ledgers-synchronized", "validation_method": "task report, scan count, config hash, coverage/export/OpenSpec/Changesets/docs checks", "execution_required": true},
      {"constraint_id": "limits-and-release-lanes-explicit", "validation_method": "scope and release-impact review", "execution_required": false},
      {"constraint_id": "security-boundary-fail-closed", "validation_method": "source/test/audit review", "execution_required": true},
      {"constraint_id": "no-publication-overclaim", "validation_method": "coverage/release/registry assertions", "execution_required": true}
    ]
  }
}
