# Refinement log — `v3-nextjs-app-router-example-archive-qa`

## Cycle 3, iteration 1 — 2026-08-03

### Delta first

Cycle 2 was rejected by a complete review. The package command's default
evidence path contradicted the implemented ledger, and provider-owned stores
could be retained after unmount by window listeners and GC intervals. This
iteration corrects both. It does not certify npm publication, stable 3.0,
untested browsers/rendering modes, or the remaining showcase portfolio.

### Specify

Retained the eight KBD/OpenSpec constraints and added explicit success criteria
for the exact no-argument command, default task-5 path, and final-owner teardown.

### Plan

Fix the blocked path, add structural coverage, introduce reference-counted core
attachments with React-effect ownership, add core/React lifecycle tests, rerun
the exact packed command, recompute hashes, and persist a third named cycle.

### Execute

- Changed the no-argument verifier default to `task-5-verification.json` and
  added a structural regression for package command, path, and task metadata.
- Made `attachGlobalListeners` return an idempotent, reference-counted disposer
  that removes window listeners and stops selected-graph GC after the last
  owner releases it.
- Moved React attachment from render-time `ensureListeners` into the
  `useGraphStoreApi` effect lifecycle.
- Added core final-owner/timer tests and a React provider-unmount test.
- Passed the exact no-argument verifier with 10/10 commands, 15/15 focused
  units, 6/6 structural tests, 12/12 isolated requests, 2/2 browser flows, and
  zero serious or critical accessibility findings.
- Recomputed all six hashes and revalidated 203 React exports, 13/13 semantic
  scenarios, 14/14 coverage tests, strict OpenSpec, Changesets, release
  contract, frozen install, diff hygiene, and security.

### Reflect

The cycle-2 BLOCK was valid and required runtime plus contract changes. Both
findings now have direct regression evidence. No critical or blocking
constraint remains, but a new full-diff adversarial review is mandatory because
the reviewed artifact changed.

### Persist

Persisted cycle-3 specification, plan, constraints, reflection, validation,
archive report, decision, and converged state under refinement ID
`07d31533-e25a-4bab-ba32-e1065c1901e2`.
