# Refinement log — `v3-tauri-universal-example-archive-qa`

## Cycle 1, iteration 1 — 2026-08-04

### Delta first

Task 4 retained planned evidence because authored tests and generated shells
were not platform execution. Task 5 then produced the missing browser,
packaged-macOS, Android-emulator, and iOS-simulator receipts. The closure
promotes only that observed boundary and keeps every broader platform,
distribution, registry, and stable-release claim excluded.

### Specify

Defined eight blocking constraints covering direct acceptance evidence, one
graph and strict layering, platform-artifact integrity, actual trust
boundaries, visual/accessibility integrity, synchronized release ledgers, the
frozen React RC lane, and truthful limits/publication state.

### Plan

Inspect representative images at original resolution; rerun focused
application, source, coverage, release-contract, skill, OpenSpec, Changesets,
artifact-hash, signed-KBD, remote-main/npm, and diff checks; then persist the
archive disposition.

### Execute

- Passed the 8-check source verifier, 7 application units, and 6 source-contract
  tests.
- Passed 13/13 semantic coverage scenarios, 15/15 coverage tests, and 16/16
  release-contract tests.
- Passed every public skill/export ledger, including Tauri 26 runtime and 57
  declarations.
- Passed strict OpenSpec and confirmed the patch Changeset remains present.
- Recomputed all 18 retained artifact hashes and confirmed five browser flows.
- Inspected representative browser, macOS, Android, and iOS images at original
  resolution.
- Confirmed signed revision 122 has 6/6 tasks and the change complete at 31/53.
- Confirmed remote main is the immutable React rc.1 SHA and npm next is absent.
- Passed diff hygiene.

### Reflect

The evidence supports archive of this bounded showcase, not certification of
Windows/Linux, physical devices, native assistive technology, signing, app
stores, registries, or stable 3.0. No speculative correction is warranted.
The next independent gate is fresh-context diff review.

### Persist

Persisted specification, plan, constraints, validation report, reflection,
decision, manifest, and converged state under refinement ID
`7d89bc27-2055-4337-a07d-3f365ad9a592`.

## Cycle 2, iteration 2 — 2026-08-04

### Delta first

The first isolated review blocked archive because both native build phases
inherited registry credentials and `project.yml` still generated version
`0.0.0`. Running XcodeGen during remediation also proved that the same source
would remove the registered iOS deep-link scheme.

### Execute

- Removed `CARGO_REGISTRY_TOKEN`, `NPM_TOKEN`, and `NODE_AUTH_TOKEN` from the
  Android Gradle `ExecSpec` and the iOS Xcode build script environment.
- Synchronized both generated iOS version fields at `0.0.1`.
- Declared the `prometheus-entity` URL scheme in `project.yml` so plist
  regeneration preserves it.
- Added four fail-closed contract regressions; 10/10 source tests pass.
- Compiled Android `buildSrc` with Gradle 8.14.3 and generated the Xcode project
  and plist with XcodeGen. The emitted build phase contains the credential
  unset and stable-Rust command.
- Re-passed typecheck, 7/7 application units, 8/8 source checks, coverage
  13/13 and 15/15, release contract 16/16, every skill ledger, strict OpenSpec,
  Changesets, 18/18 retained hashes, signed KBD revision 122, React/npm
  isolation, and diff hygiene.

### Reflect

The prior QA result was too optimistic because it validated the documented
platform commands without inspecting their inherited process boundary. Cycle
2 now traces the remediation to that real boundary and distinguishes current
generator proof from the unchanged task-5 runtime receipts.

### Persist

Updated the named artifact in place as iteration 2. A fresh isolated full-diff
review remains mandatory; the blocked findings cannot certify archive.

## Cycle 3, iteration 3 — 2026-08-04

### Delta first

The second isolated review found that cycle 2 inspected a temporary generated
Xcode project while leaving the checked-in `project.pbxproj` unchanged. That
was an evidence-boundary failure: the temporary result could not certify the
artifact consumers actually build.

### Execute

- Regenerated the checked-in Xcode project from corrected `project.yml`.
- Removed `Externals` from enumerated application sources so existing
  `libapp.a` build outputs cannot become bundle resources; framework linkage,
  library search paths, and script output paths remain.
- Made the verifier inspect the checked-in pbx shell script and reject
  `libapp.a in Resources`.
- Added two fail-closed regressions; 12/12 source-contract tests and the
  8-check verifier pass.
- Reconfirmed the generated plist version and deep-link scheme, current
  task-5 hashes, signed KBD state, and React/npm isolation.

### Reflect

Canonical artifact inspection closes the second review finding. The extra
archive-resource guard traces to an observed XcodeGen output produced while
the task-5 native library existed; it is not speculative hardening.

### Persist

Persisted cycle 3 and retained both blocked review cycles as audit history. A
new cumulative-diff review remains required.

## Cycle 4, iteration 4 — 2026-08-04

### Delta first

The third review found a real false-positive authorization proof: every
`clearGraph` failure was labeled a capability denial. Its other critical would
have duplicated canonical entity data in the platform store and therefore
conflicted with the repository's one-graph and hooks-as-store-readers rules.

### Execute

- Added an exact graph-clear capability-denial classifier using the command
  and permission identifiers observed in retained macOS/Android evidence.
- Re-threw transport and unrelated command errors instead of presenting them
  as authorization success.
- Added an eighth service unit for accepted/rejected error shapes.
- Clarified components -> hooks -> canonical graph/platform stores -> service
  ownership in the implementation evidence, example README, skill reference,
  refiner constraint, and specification.
- Extended the verifier and a thirteenth source-contract test to reject hooks
  that import the service while explicitly allowing canonical graph-store
  selection.
- Built the current Vite/stable-Rust macOS application and DMG with registry
  credentials removed; recorded executable and DMG hashes separately from the
  task-5 native runtime receipt.

### Reflect

The corrected proof composes current-source tests/build with the prior exact
native error receipt and does not relabel old binaries as current runtime
evidence. The architecture clarification preserves one graph rather than
following a reviewer suggestion that would violate it.

### Persist

Persisted cycle 4 with 8/8 constraints satisfied and retained review cycle 3
as audit history. A new cumulative-diff review remains mandatory.

## Cycle 5, iteration 5 — 2026-08-04

### Delta first

The fourth isolated review found two evidence defects: persisted queue input
was parsed only after graph hydration, and the corrected source no longer had
current Android/iOS runtime proof. Its repeated stale-progress finding targets
a generated read-only projection; signed revision 122 remains the control-plane
authority and is retained instead of rewriting generated history.

### Execute

- Moved persisted queue parsing before `startLocalFirstGraph`, so malformed
  durable input fails before entity/list hydration or seeding.
- Strengthened the malformed-queue unit to prove entities and lists remain
  empty, and added source regressions for hydration order and missing
  current-source mobile evidence.
- Built the current macOS app/DMG, Android API 36 arm64 APK, and iOS 26.5 arm64
  simulator app with registry credentials removed from child environments.
- Installed and ran Android and iOS. Android proved native SQLite state and the
  exact withheld `graph_clear` permission; iOS proved current runtime rendering.
- Added five new hash-bound task-6 artifacts while preserving all 18 historical
  task-5 artifacts and their classifications. All 23/23 hashes match.
- Re-passed 8/8 service units, 15/15 source contracts, TypeScript, ESLint,
  8/8 verifier checks, coverage 13/13 and 15/15 plus BDD, release 16/16 plus
  BDD, skills ledgers, strict OpenSpec, Changesets, signed revision 122,
  React/npm isolation, and diff hygiene.

### Reflect

The current evidence closes both valid cycle-4 findings without weakening the
one-graph architecture or claiming physical devices/signing. The stale
compatibility projection remains explicitly excluded from archive authority.

### Persist

Persisted the maximum fifth iteration with 8/8 blocking constraints satisfied.
One final isolated cumulative-diff review remains mandatory before archive.

## Continuation cycle 1, iteration 1 — 2026-08-04

### Delta first

The fifth isolated review found one residual trust-boundary defect after the
prior cycle finalized: manual `restore()` hydrated durable graph state before
validating the persisted mutation queue.

### Execute

- Parsed the manual-restore queue into a local value before graph hydration and
  assigned it only after hydration succeeds.
- Added a ninth service unit proving malformed restore input leaves entities and
  lists empty.
- Extended the verifier and source contract to reject unsafe manual-restore
  order and to reject current-source evidence after runtime/generator drift.
- Bound the task-6 receipt to the reviewed runtime/generator bundle with SHA-256
  `4af538762bf7feb2bea8d8fa7616708b9539400056b193923e3c381da17c48c5`.
- Rebuilt macOS, Android API 36 arm64, and iOS 26.5 arm64 simulator targets with
  registry credentials removed from children. Reinstalled and ran Android and
  iOS; Android again proved the exact withheld graph-clear permission.
- Refreshed the five task-6 runtime artifacts and passed 9/9 service units,
  17/17 source contracts, verifier, TypeScript, ESLint, and 23/23 hashes.

### Reflect

The correction closes the observed manual-restore path without adding another
graph, weakening platform classifications, or changing any public API. The
source-bundle binding prevents the same stale-receipt condition from silently
passing again.

### Persist

Persisted continuation cycle 1 with 8/8 blocking constraints satisfied. A new
isolated cumulative-diff review remains mandatory before archive.

## Continuation cycle 2, iteration 1 — 2026-08-04

### Delta first

The sixth isolated review reported the Gradle wrapper JAR and iOS resources as
missing because the review tree omitted binary and raster paths. Direct Git
inspection proved the wrapper, storyboard, and asset catalog are tracked; only
the source-empty Tauri web-assets directory is generated at build time.

### Execute

- Required the Gradle wrapper JAR, launch storyboard, and asset-catalog root in
  the eight-check source verifier.
- Required both generator and checked-in Xcode declarations for the Tauri web
  assets resource directory.
- Expanded the current-source binding from five text generators to 27 text and
  binary source/resource files with SHA-256
  `ba59cdcf9b1378ce1de5d18b930c7c5cc5229504e14e4a2e47b4f4701860d75a`.
- Extended the existing missing-shell regression across the wrapper,
  storyboard, and asset catalog while retaining 17/17 source tests.
- Removed the empty generated `assets/` directory and prior export directory
  before a credential-scrubbed iOS build. Tauri recreated the directory, Xcode
  compiled/copied resources, and the simulator bundle completed.
- Re-passed 9/9 service units, 17/17 source contracts, the verifier, focused
  ESLint, tracked-resource checks, and the receipt hash assertion.

### Reflect

The review was correct that its packet did not prove the referenced resources,
but its source conclusion was overbroad. The correction makes the distinction
machine-visible and verifies clean-directory behavior rather than adding an
unnecessary placeholder to a generated folder.

### Persist

Persisted continuation cycle 2 under refinement ID
`04b8ea1d-388b-4eeb-b451-0ba5ac6dc0a0` with 8/8 constraints satisfied. A new
isolated cumulative-diff review remains mandatory before archive.

## Continuation cycle 3, iteration 1 — 2026-08-04

### Delta first

The seventh isolated review found a real evidence gap: the main webview denied
both destructive clear and removal by capability, but current runtime proof
invoked only clear. One native denial cannot certify a different command.

### Execute

- Added an exact graph-removal capability classifier alongside the existing
  clear classifier and made the proof invoke both commands sequentially.
- Added a native mock-host test for withheld `graph_remove_entity` and expanded
  the current Android UI to visibly prove both denial messages.
- Added exact positive/negative unit coverage for both denial shapes and a
  source-contract rejection for incomplete removal evidence.
- Expanded the current-source bundle from 27 to 30 files so it includes the
  dashboard, native host, and capability manifest; SHA-256 is
  `ebb76b5637a5e87b9e1a6f80b3f877705beaa66046730e9ec76ee574b0817fc7`.
- Rebuilt the optimized macOS app/DMG, Android API 36 arm64 APK, and iOS 26.5
  arm64 simulator app with registry credentials removed from build children.
- Reinstalled and ran Android/iOS. Android returned exact clear and removal
  denial pairs; iOS rendered after normal webview startup. Refreshed all five
  current runtime artifacts and their hashes.
- Passed 9/9 application units, 19/19 source contracts, the 8-check verifier,
  TypeScript, scoped ESLint, and the receipt hash assertion.

### Reflect

The correction proves the complete real authorization boundary and does not
broaden the platform or publication claim. The initial blank iOS capture was
discarded; only the later visibly rendered runtime image is retained.

### Persist

Persisted continuation cycle 3 under refinement ID
`bbf0e491-d37a-406d-8a6c-ad82d198b121` with 8/8 constraints satisfied. A new
isolated cumulative-diff review remains mandatory before archive.
