# Participant results

Place one anonymized JSON file per participant here. Filenames should match the
record's anonymous `participantId`, for example `p-a01.json`.

Do not commit names, email addresses, employer names, screen recordings,
application data, or free-form participant biography. The evaluator rejects
records with unexpected fields and ignores this README.
