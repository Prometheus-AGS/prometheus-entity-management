---
title: Evidence gallery
sidebar_position: 1
---

import EvidenceFigure from '@site/src/components/EvidenceFigure';

# Evidence gallery

Every figure below resolves to an allowlisted source, scenario IDs, verification receipt, content-addressed Git blob SHA, dimensions, certification status, and SHA-256 in [`manifest.json`](pathname:///evidence/manifest.json). Select an image to download its original PNG.

## React and Next.js

<EvidenceFigure assetId="react-graph-transports" alt="Prometheus React showcase with normalized graph list and detail panels beside REST and GraphQL transport controls" caption="React/Vite joins normalized Task identities across graph views and deterministic REST/GraphQL transport modes." width={1280} height={720} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="react-mutations-realtime" alt="React showcase mutation and realtime panels displaying optimistic and coalesced cross-view state" caption="Optimistic confirmation, exact rollback, and coalesced realtime changes update one React graph." width={1280} height={720} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="react-offline-lifecycle" alt="React showcase offline lifecycle and accessibility evidence panels" caption="Browser PGlite persistence, Loro convergence, lifecycle state, and accessibility share one React release surface." width={1280} height={720} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="next-ssr-hydration" alt="Next.js App Router showcase displaying server-rendered entity state after scoped client hydration" caption="Next.js request-isolated server graphs hydrate one scoped client graph before realtime takeover." width={1280} height={720} smallWidth={640} largeWidth={1280} />

## Agentic A2UI

<EvidenceFigure assetId="a2ui-policy-approval" alt="Agentic A2UI showcase displaying a policy-controlled action and human approval state" caption="A catalogued A2UI action crosses application policy and human approval before graph mutation." width={1280} height={2193} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="a2ui-malformed-denial" alt="Agentic A2UI showcase displaying a rejected malformed artifact" caption="Malformed A2UI content fails closed before an unknown component or action reaches the graph." width={1280} height={1535} smallWidth={640} largeWidth={1280} />

## Flutter and provenance

<EvidenceFigure assetId="flutter-cross-view-initial" alt="Flutter golden showing list and detail projections of one normalized task" caption="Flutter list and detail providers resolve the same canonical Task before a local patch." width={2880} height={1800} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="flutter-cross-view-optimistic" alt="Flutter golden showing an optimistic task update in list and detail views" caption="A Flutter optimistic overlay is visible through list and detail Riverpod joins while canonical data remains separate." width={2880} height={1800} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="flutter-a2ui-phone" alt="Flutter phone golden displaying a policy-controlled A2UI surface" caption="The Flutter phone surface renders the allowlisted GenUI catalog inside the same entity application." width={430} height={932} smallWidth={430} largeWidth={430} />

<EvidenceFigure assetId="flutter-source-lineage" alt="Flutter source provenance diagram showing retained history and canonical package ownership" caption="Allowlisted KnowMe Flutter lineage is retained as provenance while this repository owns the canonical Dart package." width={1200} height={680} smallWidth={640} largeWidth={1200} />

## Tauri desktop and mobile

<EvidenceFigure assetId="tauri-desktop-graph" alt="Tauri desktop showcase displaying entity graph views and native platform adapter status" caption="The Tauri desktop projection keeps native adapter status beside the shared normalized graph." width={1280} height={1029} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="tauri-android-runtime" alt="Android emulator showing the Prometheus Tauri entity application runtime result" caption="Android API 36 emulator runtime evidence for the shared Tauri application and allowed capability path." width={1344} height={2992} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="tauri-ios-runtime" alt="iOS simulator showing the Prometheus Tauri entity application runtime result" caption="iOS 26.5 simulator runtime evidence for the same Tauri domain and capability contract." width={1206} height={2622} smallWidth={640} largeWidth={1206} />

## Flint and release operations

<EvidenceFigure assetId="release-disposition" alt="Release diagram showing certified code and evidence with npm publication still blocked" caption="The RC release disposition separates change certification from npm publication authority." width={1400} height={960} smallWidth={640} largeWidth={1280} />

<EvidenceFigure assetId="flint-realtime-contract" alt="Diagram of Flint Realtime Fabric flowing through the portable adapter and realtime manager into list and detail graph views" caption="Flint watch events become coalesced ChangeSets under explicit tenant, channel, consumer, checkpoint, and key boundaries." width={2800} height={1600} smallWidth={640} largeWidth={1280} />
