# Tasks: v2-devtools-time-travel

- [ ] Add "Timeline" tab to EntityExplorer panel + tab registry
- [ ] Subscribe to graph-action records via devtools-event-bus
- [ ] Render chronological action list with selection
- [ ] Compute + render before/after state diff per action (reuse exportGraphSnapshot)
- [ ] Snapshot export (download JSON) + import (load into inspection view)
- [ ] Pin-to-entity filter (type:id)
- [ ] Tests: timeline renders actions; diff correct; export/import roundtrip
- [ ] `pnpm run typecheck` + `pnpm run test`
