# Tasks: v4-bucket-assigner

- [x] Create `crates/pes-rules/src/assigner.rs` with `BucketAssigner` struct
- [x] Implement `BucketAssigner::new(rule_set: Arc<SyncRuleSet>, pool: PgPool, cache_ttl: Duration) -> Self` — **revised to `-> Result<Self, ParseError>`** during security review (see task 9 below): validates the rule set via `pes_rules::validate` before construction, so it is structurally impossible to build an assigner from a `SyncRuleSet` that fails semantic validation
- [x] Implement `BucketAssigner::assign(&self, claims: &TokenClaims) -> Result<Vec<BucketAssignment>, SyncError>`
  - [x] Check `claims.exp > now` — reject expired JWTs immediately
  - [x] Check cache: `DashMap<(sub, rule_version), (Vec<BucketAssignment>, Instant)>`
  - [x] For each rule, execute parameter queries via `sqlx::query` with `$1 = claims.sub` (runtime API, not `query_as!`, so `cargo build` doesn't require a live `DATABASE_URL` at compile time)
  - [x] Substitute resolved parameters into data queries using safe template engine
  - [x] Store result in cache with TTL, return assignments
- [x] Create `TemplateEngine` in `src/template.rs`: substitutes `{bucket_parameters.X}` with validated values
  - [x] Allowlist: values must match `^[a-zA-Z0-9_\-]{1,128}$` (UUIDs, slugs, integers as strings)
  - [x] Return `SyncError::BucketAssignmentFailed` if any value fails validation (assigner) / `ParseError::Validation` (template module directly)
  - [x] Unit test: valid UUID passes; `'; DROP TABLE' ` rejects
- [x] Write exhaustive test matrix (all 10 scenarios from proposal)
- [x] Add `proptest` dev-dependency
- [x] Write proptest property: `∀ s: String. assign({ sub: s }) never executes SQL containing s literally`
  - [x] Uses a manually-managed Docker `postgres:16-alpine` container (`pes-test-pg`, port 55432) rather than `sqlx::test` or the `testcontainers` crate — same isolation goal (ephemeral, reproducible schema), simpler to wire into this workspace without adding another dev-dependency. `PES_TEST_DATABASE_URL` env var overrides the connection string for CI. Tests skip gracefully (return `Ok(())` / early-return) if no DB is reachable, rather than failing the whole suite.
- [x] Run `cargo llvm-cov --html` and verify 100% branch coverage on `assign()` — 12/12 branches covered in `assigner.rs` (confirmed via `--branch --show-missing-lines`; report excludes the 10K-case `assigner_proptest` run, which is prohibitively slow under coverage instrumentation — the exhaustive `assigner_matrix` suite plus 3 added defensive-branch tests (missing parameter_queries entry, unsupported column type, unsafe DB-sourced value) achieve full branch coverage on their own)
- [x] Run security-reviewer agent on `assigner.rs` before marking done — reviewed `assigner.rs`, `template.rs`, `parser.rs`, `validator.rs`, `error.rs`, `types.rs`, and both test files. **No critical or unresolved high-severity SQL injection issues.** Confirmed: no `format!`/string-concat in any SQL path; `$1` binding used exclusively for JWT `sub`; `{bucket_parameters.X}` substitution allowlist-validated with no bypass, including for values sourced from Postgres (not just raw JWT claims); expiry checked before any DB call; no cross-user cache-key collision possible. One HIGH finding fixed immediately: `BucketAssigner::new` accepted an unvalidated `SyncRuleSet` (validator.rs checks were dead code from the runtime's perspective) — fixed by making `new()` fallible and calling `validate()` internally. Two MEDIUM/LOW forward-looking design notes deferred to later changes (not blocking): (1) `SyncError` variants should be redacted before reaching a client-facing API response once `pes-gateway` implements that boundary; (2) the `DashMap` cache has no eviction/sweep mechanism yet — a high-cardinality attacker with many validly-signed tokens could grow it unboundedly over time; add a TTL sweep before production rollout.
  - [x] **Follow-up (post-archive):** Both deferred findings closed. (1) Added `crates/pes-gateway/src/error.rs`: a `GatewayErrorResponse`/`GatewayErrorCode` redaction boundary with `From<&SyncError>`/`From<SyncError>` conversions — `Database`, `LsnGap`, and `ChecksumMismatch` collapse to a generic "internal error" message (no query text, driver detail, or internal LSN/checksum state exposed); `AuthError`/`BucketAssignmentFailed`/`ProtocolError` map to static, generalized client-safe messages. `pes-gateway` itself remains a stub otherwise (full WebSocket server is still `v4-pes-gateway`'s scope) — this only adds the redaction module + 7 unit tests so the boundary exists and is enforced by construction once the gateway starts serializing errors. (2) Added `BucketAssigner::sweep_expired_entries(&self) -> usize` (synchronous, directly testable — removes all `DashMap` entries past `cache_ttl` via `retain`) and `BucketAssigner::spawn_cache_sweeper(self: &Arc<Self>, interval: Duration) -> JoinHandle<()>` (background `tokio::time::interval` loop calling the sweep; caller owns the handle and aborts it on shutdown) to `crates/pes-rules/src/assigner.rs`. 3 new integration tests added to `assigner_matrix.rs` covering sweep eviction, live-entry preservation, and the background sweeper task.
- [x] Verify `cargo clippy -- -D warnings` passes (particularly: no `format!` in SQL paths)
