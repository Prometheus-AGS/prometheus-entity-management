# Tasks: v4-wal-to-bucket-router

- [x] Create `crates/pes-gateway/src/router.rs` (or separate `pes-router` crate if size warrants) — **created as a separate `pes-router` crate**, per the task's own alternative and confirmed with the user: a concurrent background session (task_81d7bf99) is actively editing `pes-gateway/src/lib.rs` and `Cargo.toml` (uncommitted), and editing the same files risked a merge conflict. A new crate sidesteps the overlap entirely; `pes-gateway` can depend on `pes-router` later. Added to the workspace members list and `[workspace.dependencies]` (`pes-router`, plus `frf-broker-iggy` and `prometheus`, both newly required)
- [x] Implement `WalToBucketRouter::run()` — consume `frf-postgres-cdc` ChangeEvent stream — **deviates from the proposal's literal design**: `frf-postgres-cdc` has no `ChangeEvent` type and `PostgresCdcConsumer::run_until_shutdown` publishes decoded WAL changes to an injected `frf_ports::LogBroker`, not a pull `Stream` (see `consumer.rs`). `run()` instead subscribes to that broker's channel via `LogBroker::subscribe`, which returns the real `EventStream` — documented at length in `router.rs`'s module doc comment, including the LSN caveat: the true Postgres LSN is captured internally in `frf-postgres-cdc` (`consumer.rs:127`) but only used for WAL feedback ack, never propagated to the published `EventEnvelope`. The envelope's `Offset` (broker-local monotonic counter) is used as `PgLsn` instead — confirmed with the user this is acceptable since `pes-oplog` only requires monotonicity, not true WAL-position semantics, for its ordering guarantees
- [x] Implement `BucketAssigner::find_affected_buckets(&change) -> Vec<BucketId>` (local eval, no DB roundtrip for cached rules) — implemented in `pes-rules/src/assigner.rs` (not `pes-router`, since it needs access to `BucketAssigner`'s private cache). **Scope confirmed with the user**: local evaluation covers the single-owner-column pattern used throughout `docs/sync-rules-reference.md` (match `entity_type` against a cached assignment's `data_queries` keys, then check equality between any resolved parameter value and any top-level scalar field in the changed row — e.g. `owner_id`), NOT a general SQL `WHERE`-clause evaluator. Explicitly documented as a named limitation in the method's doc comment: multi-column filters, ranges, and joins are not handled and would require re-verification via `assign()` (a real Postgres round-trip). Validated with a temporary smoke test (removed after passing) proving correct matches, non-matches, and entity-type isolation
- [x] Fan-out: `tokio::spawn` one task per affected bucket for concurrent oplog writes — real `tokio::spawn` per bucket (not `futures::join_all` over local async closures, which was the first draft), so a panic in one bucket's append is isolated via `JoinError` rather than poisoning the others, and writes are schedulable across worker threads, not just cooperatively interleaved on one
- [x] Backpressure: bounded channel (capacity 1000) between WAL consumer and oplog writer
- [x] Add `metrics` module: `prometheus` crate counters/histograms for the 4 metrics above
- [x] E2E integration test (testcontainers): INSERT into `entities` table → verify correct bucket receives op — real Postgres container configured for logical replication (`wal_level=logical`, custom `GenericImage` command since `testcontainers-modules`'s default Postgres image doesn't enable it), real `PostgresCdcConsumer` decoding real WAL, bridged via an in-process mock `LogBroker` (following the same pattern FRF's own `cdc_integration.rs` uses, scoping the test to the WAL→router→oplog path rather than needing a real Iggy server too) to a real `WalToBucketRouter` and real `BucketOpLog`. **Found and fixed a real test-fixture bug during this task**: FRF's `frf-postgres-cdc` decoder hard-requires every watched table's `id` primary key to parse as a UUID (`frf_domain::EntityId`); using a plain string `"entity-1"` caused rows to be silently skipped with a WARN-level log, not a hard error, which made the first test run fail with no obvious cause until `RUST_LOG=debug` was enabled. Fixed by using real UUIDs for `entities.id`/`unrelated_table.id`. Passes in ~2.6s wall clock (INSERT-to-detection latency ~300ms, well under the 100ms-ish expectation once slot startup is excluded)
- [x] E2E integration test: INSERT into unrelated table → verify zero ops appended — passes; `find_affected_buckets`'s `entity_type` match correctly rejects the row since no cached assignment's `data_queries` has an `unrelated_table` key. Both E2E tests run together cleanly (9.24s total, separate containers, no shared state)
- [x] Backpressure test: slow oplog mock → verify WAL consumption rate drops — **deviates from a literal "slow oplog mock"**: `BucketOpLog` is a concrete struct, not a trait, so there's no seam to inject a mock into the full `WalToBucketRouter::run()` pipeline without a larger refactor. Instead tests the exact `tokio::sync::mpsc::channel(BACKPRESSURE_CAPACITY)` primitive `run()` actually uses (imported via the crate's public `BACKPRESSURE_CAPACITY` constant), paired with a synthetic slow consumer standing in for a slow oplog writer. Two tests: (1) proves a full channel blocks `send()` until drained, (2) proves end-to-end producer throughput is bounded by a slow consumer's rate (50 items × 20ms consumer delay, elapsed time asserted ≥ 80% of the theoretical minimum) — i.e. the producer cannot outrun the consumer and buffer unboundedly. Stable across 4 runs, no flakiness observed
- [x] Verify `cargo clippy -- -D warnings` passes
