# Tasks: v2-devtools-graph-visualization

- [ ] Add "Graph" tab to EntityExplorer panel + tab registry
- [ ] Build node set from entities; edge set from crud/relations registry
- [ ] Implement lightweight SVG/canvas layout (force or tree)
- [ ] Node click → shared detail pane (selection context from C5)
- [ ] Node-count cap with visible "showing N of M" notice (no silent truncation)
- [ ] Tests: nodes/edges derived correctly; click selects; cap notice renders
- [ ] `pnpm run typecheck` + `pnpm run test`
