# Verification — `fix-atomic-fetched-list-ingestion`

Date: 2026-08-23

Implementation baseline: `origin/main` at
`e25210010a8eb4e575f7e4fc6e04be598a8c8213`, whose public package manifests are
3.0.2. Signed release negative control: `v3.0.2` at
`f29a701649799df3ff64f5f986e3c016246d34b6`.

Scope: framework-neutral core and React package source on the local macOS host,
plus packed ESM, CommonJS, and declaration artifacts. Results transfer to no
published npm artifact, browser application, other binding runtime, or deployed
consumer. No aggregate release verdict is made.

| requirement | assertion observed | negative control observed | command and observed output | source SHA | profile | limit |
|---|---|---|---|---|---|---|
| Preserve the signed 3.0.2 defect evidence. | The retained fixture ran against a clean detached checkout of `v3.0.2` with 7,248 rows and verified that checkout `HEAD`, the resolved tag, and the supplied full SHA were identical before importing source. | The control observed exactly 7,250 success publications, N+2. Supplying the prior untagged SHA failed immediately with a source-mismatch error. | `pnpm exec tsx .prometheus/evidence/fix-atomic-fetched-list-ingestion/reproduce-3.0.2-negative-control.ts /Users/gqadonis/.claude/worktrees/entity-management-302-negative-control/packages/entity-graph-core/src/graph.ts f29a701649799df3ff64f5f986e3c016246d34b6` → `{"sourceRelease":"3.0.2","sourceSha":"f29a701649799df3ff64f5f986e3c016246d34b6","rows":7248,"successPublications":7250,"expectedNPlus2":7250}`. | `f29a701649799df3ff64f5f986e3c016246d34b6` | Signed core v3.0.2 source | Direct vanilla-store control, not a browser timing claim. Setup, raw output, and mismatch control are retained in `.prometheus/evidence/fix-atomic-fetched-list-ingestion/negative-control.json`. |
| Inventory every production split-write owner. | Six owners were classified: core `fetchList`; React `useEntities`; React `useEntityQuery`; legacy `useEntityView`; GraphQL normalization/list hook; Electric/PGlite adapter. All now call `ingestFetchedList`. | The final production search finds no bulk or GraphQL list normalization path followed by per-row `setEntityFetched`; remaining granular occurrences are single-entity, realtime, or CRUD writes. | `rg -n "upsertEntities|setEntityFetched|ingestFetchedList" <seven production files>` → atomic action at every classified list-success path. | baseline + change | Core/React production source | Test fixtures are excluded. Inventory retained in `.prometheus/evidence/fix-atomic-fetched-list-ingestion/production-path-inventory.md`. |
| Atomically ingest rows, lifecycle, lists, and projections. | One action captures one timestamp, snapshots pre-ingestion merge origins, merges primary and side-descriptor batches, completes entity/sync lifecycle, updates replace/append lists, applies filter/search/sort projections, and completes selected list fetch state inside one Immer mutation. | A side-batch merge strategy throws after the primary merge is attempted; the control observes zero publications and no primary entity or list state. | `pnpm --filter @prometheus-ags/entity-graph-core exec vitest run src/ingest-fetched-list.test.ts` → 1 file passed, 11 tests passed. | baseline + change | Core 3.0.3 source | Local in-process store transaction; no cross-process transaction claim. |
| Preserve append, duplicate-ID, empty-page, and projection-order semantics. | Existing target-list IDs stay first, new target-list IDs append in response order, duplicate IDs deduplicate while data merges sequentially, repeated client-origin IDs keep that origin for every merge callback, and empty replacements clear IDs. Cursor base projections retain the prior subscriber's start-insertion ordering. | The duplicate-origin control would observe `client,server` if lifecycle metadata changed between merges; it observed `client,client`. The cursor-hook case would fail if the atomic projection switched to end placement. | Same core command → 11/11 passed; the actual cursor-hook case is included in the React command below. | baseline + change | Core 3.0.3 source | Stable list/projection behavior only; unrelated realtime ordering is outside scope. |
| Make publication count independent of row count. | Separate 1, 12, and 7,248-row ingestions each emitted exactly one complete success publication. | The matched 3.0.2 control emitted 7,250 publications for 7,248 rows. | Same core command → 1 file passed, 11 tests passed; the parameterized 1, 12, and 7,248-row cases all passed. | baseline + change | Local core store | Counts store publications; no general latency or browser-main-thread budget. |
| Route core `fetchList` without partial success. | Public `fetchList` emitted fetch-start plus one complete success, invoked side effects and `onSuccess` after state completion, retried without partial graph state, and left entities untouched on final failure while reporting the list error. | One fixture fails before succeeding; another always fails and asserts that no entity bucket is created. | Same core command → all three `fetchList atomic success boundary` cases passed. | baseline + change | Core 3.0.3 source | Existing transport-specific cancellation behavior was preserved in source but not newly certified. |
| Migrate actual React, GraphQL, and adapter paths. | Real `useEntities`, paginated `useEntityQuery`, remote `useEntityView`, `useGQLList`, and PGlite adapter flows each reached one complete success snapshot independent of row count. GraphQL list membership still follows `getItems`, while normalization retains every descriptor row; empty lists and missing entities complete their fetch lifecycle. | The paginated, remote-view, and GraphQL fixtures use 12-row responses and fail if projection or list completion produces multiple complete success snapshots. A throwing side descriptor observes no partial primary publication; a null entity response must not remain fetching. | `pnpm --filter @prometheus-ags/prometheus-entity-management exec vitest run src/hooks/use-entity-query.test.ts src/view/use-entity-view.test.tsx src/graphql/hooks-atomic.test.tsx src/hooks/use-entities-atomic.test.tsx src/adapters/electricsql-react.test.tsx` → 5 files passed, 23 tests passed. | baseline + change | React 19/jsdom with the real hooks and graph store | Local jsdom integration only; no deployed browser or non-React binding runtime. |
| Keep core and React source statically valid. | Both package typechecks and scoped ESLint completed with no diagnostics. | Not a fail-closed product assertion; behavioral negative controls are recorded in the rows above. | Core typecheck → exit 0; React typecheck → exit 0; scoped `pnpm exec eslint ... --max-warnings 0` → exit 0 with no output. | baseline + change | Core/React TypeScript | Touched packages/files only; no workspace-wide lint/typecheck claim. |
| Preserve packed ESM, CommonJS, declaration, and export-ledger compatibility. | Core and React 3.0.3 builds succeeded. AreTheTypesWrong found no problems for either packed package across Node 10, Node 16 CJS, Node 16 ESM, and bundler resolution. Publint exited successfully; runtime export ledgers matched; the type-only atomic API is documented in the shared agent API reference. | Packed gates fail on missing or unresolvable module/declaration entrypoints; export verification fails on runtime-ledger drift. No such failure was observed. | Package builds → exit 0; strict ATTW commands → `No problems found`; publint → exit 0 with only the pre-existing repository-URL suggestion; `verify:skills` → core 123 and React 203 runtime exports match ledger. | baseline + change | Packed core/React 3.0.3 artifacts | Packed and inspected locally; not published or installed from npm. |
| Version the fixed 3.x group consistently. | The standard fixed-group changeset advanced all 12 public packages from 3.0.2 to 3.0.3 and updated affected private dependents; post-version status has no pending package bump. | A pre-existing empty Next.js changeset was consumed by the version command and restored byte-for-byte so this change does not steal unrelated history. | `pnpm changeset status` → no packages to bump; manifest inventory → all 12 public packages at `3.0.3`. | baseline + change | Repository release metadata | No npm publish, tag, dist-tag move, or registry verification occurred. |
| Pass isolated adversarial review. | The artifact-only critic returned APPROVE with no finding after two correction rounds; the independent judge returned APPROVE after the release-tag evidence correction. | Earlier critic passes blocked GraphQL partial commits, projection writes/order, duplicate origins, missing lifecycle controls, and placeholder evidence. The first judge pass rejected the untagged SHA. Each control failed before the corresponding correction. | Final critic → `APPROVE (0 critical, 0 warning, 0 suggestion)`; final judge → `APPROVE — no remaining blocker`. | baseline + change | Artifact review | Reviews assess this local source/evidence artifact only; they do not certify an npm publication. |
| Keep OpenSpec and workflow scope valid. | Strict validation and diff hygiene pass; GitHub Actions are unchanged. | Not a product fail-closed assertion. | `openspec validate fix-atomic-fetched-list-ingestion --strict` → valid; `git diff --check` → exit 0; workflow diff check → no output. | baseline + change | OpenSpec/process | Commit, push, and PR evidence remains pending until task 4.4 completes. |

## Unverified limits

- No npm package was published; registry `latest` remains untouched.
- UAR remains intentionally pinned to release 3.0.2 during its current phase.
  This upstream 3.0.3 source is not silently substituted into UAR.
- No Svelte, Solid, Alpine, HTMX, Web Components, Tauri, A2A, A2UI, Flutter,
  or deployed-browser runtime was exercised.
- No broad workspace, soak, load, or GitHub Actions product test run was performed.
